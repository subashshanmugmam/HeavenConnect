import 'package:flutter/material.dart';

class FoodSharingPage extends StatelessWidget {
  const FoodSharingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Food Sharing')),
      body: const Center(
        child: Text('Food Sharing Page - Coming Soon'),
      ),
    );
  }
}
